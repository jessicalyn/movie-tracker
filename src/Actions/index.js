export const addMovies = (movies) => ({
  type: "ADD_MOVIES",
  movies
})

export const addUser = (user) => ({
  type: "ADD_USER",
  user
})

export const loginUser = (email) => ({
  type: "LOGIN_USER",
  email
})
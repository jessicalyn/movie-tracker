export const addMovies = (movies) => ({
  type: "ADD_MOVIES",
  movies
})

export const addUser = (users) => ({
  type: "ADD_USER",
  users
})

export const loginUser = (email) => ({
  type: "LOGIN_USER",
  email
})